<%- include('partials/head', { title }) %>
<%- include('partials/navbar') %>

<main class="container-shell py-20">
  <section class="mx-auto max-w-xl panel p-8 text-center">
    <p class="inline-flex rounded-full bg-red-50 px-3 py-1 text-xs font-semibold uppercase tracking-wider text-red-700">Error 404</p>
    <h1 class="mt-4 text-3xl font-extrabold">Pagina no encontrada</h1>
    <p class="mt-3 text-sm text-slate-600">La ruta que buscas no existe o no esta disponible.</p>
    <div class="mt-7">
      <a href="/" class="btn-main">Volver al inicio</a>
    </div>
  </section>
</main>

<%- include('partials/footer') %>
